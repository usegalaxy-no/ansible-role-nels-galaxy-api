---
# defaults file for ansible-role-nels-galaxy-api
nels_galaxy_api_user: galaxy
nels_galaxy_api_group: galaxy
nels_galaxy_api_root: /srv/nels-galaxy-api
nga_root: /srv/nga
nga_configdir: "{{ nga_root }}/configs"
nels_galaxy_config: nga-server.json
nels_galaxy_tmp_dir: "/srv/tmp/"
#nels_galaxy_api_port: 8008
nels_galaxy_api_log_dir: /var/log/nga
galaxy_config_file_location: "/srv/galaxy/config/galaxy.yml"
#nga_tmp_dir: /srv/tmp/

# Need to set and is generally sat elsewhere in the usegalxy-no playbooks:
#"nels_storage_client_key":
#"nels_storage_client_secret":
#"nels_url":
#"nels_storage_url":
# nga_master_galaxy_url:
# nga_master_url:



